package commands

object Commit {

  def apply(): Boolean = {
    //recuperer les fichiers depuis l'index
    //construis les trees et blobs
    //construis l'objet commit qui pointe sur le tree
    //recuperer la branche courante dans le HEAD
    //ajouter le commit a la branche
    true
  }
}
