/*
import classes.{Blob, Commit, Index}
import org.scalatest.FlatSpec

class CommitTest extends FlatSpec{

  "toString" should "return a String that representing elements separated by spaces" in{
    val commit = Commit("a", "b", "c", "d", Index(List()))
    assert(commit.toString == "a b c d")
  }

  "addToObjects" should "return true if the operations succeded" in{

  }

  "addToObjects" should "return false if the operations failed" in{

  }
}*/
